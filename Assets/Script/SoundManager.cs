using UnityEngine;

public class SoundManager : MonoBehaviour
{
    public enum AllSound
    {
        Background,
        ClickButton,
        WinGameSound,
        LoseGameSound,
        WarningTime,
        WarningHitFossil,
        Hammer,
        Drill,
        Brush,
        Glue
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
